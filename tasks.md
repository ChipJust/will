# will — Tasks

Actionable items that tools can create and update automatically.
Manual entries welcome. Format: checkbox with metadata in parentheses.

- [ ] **Statusline errors** — 3 errors in `~/.claude/statusline-debug.log` (checked 2026-06-27). Debug `will/tools/statusline.py`.
